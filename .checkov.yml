# The labs are intentionally isolated teaching examples. Production IaC lives in
# bootstrap/, modules/, and environments/ and is scanned on every relevant change.
skip-path:
  - labs
  - node_modules

# AWS KMS key policies require Resource "*". These three checks are waived only
# for the state-key policy, where explicit principals are the real boundary.
# Exception review expires 2027-09-18; remove it when Checkov models this AWS
# KMS requirement without treating its required wildcard resource as a finding.
skip-check:
  - CKV_AWS_109
  - CKV_AWS_111
  - CKV_AWS_356
