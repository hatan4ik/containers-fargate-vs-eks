# EKS development environment

This root contains one pinned platform-module call. Copy the example files outside
source control and run a plan with the development GitHub Environment OIDC role.
No moved block belongs here until current state and a successful baseline plan are
available.
