# Fargate development environment

This root contains one pinned platform-module call. Copy the two `*.example`
files outside source control, supply approved values, and run a plan with the
development GitHub Environment OIDC plan role. No moved block belongs here until
the current state and a successful baseline plan are available.
