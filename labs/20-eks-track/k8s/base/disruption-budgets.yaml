apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: users
  namespace: z2h
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: users
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: orders
  namespace: z2h
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: orders
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: gateway
  namespace: z2h
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: gateway
